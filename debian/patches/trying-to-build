Description: <short summary of the patch>
 TODO: Put a short summary on the line above and replace this paragraph
 with a longer explanation of this change. Complete the meta-information
 with other relevant fields (see below for details). To make it easier, the
 information below has been extracted from the changelog. Adjust it or drop
 it.
 .
 machinekit-cnc (0.1.1581883713.gite10a5529f-1~buster) experimental; urgency=low
 .
   * Travis CI rebuild
     - TAG amd64_10
     - PR/branch debian-buster-build
     - Commit e10a5529f
     - https://github.com/ArcEye/machinekit-cnc/commit/e10a5529f
Author: Jakub Fišer <jakub.fiser@eryaf.com>

---
The information above should follow the Patch Tagging Guidelines, please
checkout http://dep.debian.net/deps/dep3/ to learn about the format. Here
are templates for supplementary fields that you might want to add:

Origin: <vendor|upstream|other>, <url of original patch>
Bug: <url in upstream bugtracker>
Bug-Debian: https://bugs.debian.org/<bugnumber>
Bug-Ubuntu: https://launchpad.net/bugs/<bugnumber>
Forwarded: <no|not-needed|url proving that it has been forwarded>
Reviewed-By: <name and email of someone who approved the patch>
Last-Update: 2020-02-17

--- /dev/null
+++ machinekit-cnc-0.1.1581883713.gite10a5529f/bashsc
@@ -0,0 +1,3 @@
+#!/bin/bash
+alias docker='podman'
+source $1 ;
--- /dev/null
+++ machinekit-cnc-0.1.1581883713.gite10a5529f/log.txt
@@ -0,0 +1,11 @@
+++ exec docker run --rm -e DEB_BUILD_OPTIONS=parallel=8 -e DH_VERBOSE= -e DEBIAN_SUITE= -e MAJOR_MINOR_VERSION= -e PKGSOURCE= -e REPO_URL= -e TRAVIS_BRANCH= -e TRAVIS_PULL_REQUEST= -e TRAVIS_REPO= -e TRAVIS_REPO_SLUG= -u 1000:1000 -e USER=mars -v /home/mars:/home/mars -e HOME=/home/mars -v /home/mars/Documents/Programming/machinekit:/home/mars/Documents/Programming/machinekit -w /home/mars/Documents/Programming/machinekit/cmk-cnc -e TAG=amd64_8 -h arceye-mk-cross-builder-amd64_8 arceye/mk-cross-builder:amd64_8 bash -xec '
+            # configure source package
+            debian/configure -prx;
+
+            # update Changelog and build source package
+            /bin/build_source_package false
+
+            # build binary packages
+            dpkg-buildpackage -uc -us -b -j8
+            '
+bash: exec: docker: not found
--- machinekit-cnc-0.1.1581883713.gite10a5529f.orig/scripts/build_docker
+++ machinekit-cnc-0.1.1581883713.gite10a5529f/scripts/build_docker
@@ -81,6 +81,7 @@ BIND_SOURCE_DIR="$(readlink -f $PWD/..)"
 
 # Directory containing this script
 SCRIPT_DIR="$(dirname $0)"
+SCRIPT_DIR=scripts
 
 # Make TAG accessible to called programs
 export TAG
@@ -119,6 +120,7 @@ case $CMD in
             debian/configure ${PKG_CONFIGURE_ARGS};
 
             # update Changelog and build source package
+			echo $SCRIPT_DIR
             $SCRIPT_DIR/build_source_package $BUILD_SOURCE
 
             # build binary packages
